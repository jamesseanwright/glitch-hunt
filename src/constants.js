(function () {
    'use strict';

    /* string property accessor is
     * workaround for Closure Compiler */
    window['G'] = {
        constants: {
            SCALE: 4,
            WORLD_WIDTH: 1,
            WORLD_HEIGHT: 1
        }
    };
}());