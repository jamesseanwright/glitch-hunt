'use strict';

function RectRenderable(stroke, border, x, y, width, height) {
    this.stroke = stroke;
    this.border = border;
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
}