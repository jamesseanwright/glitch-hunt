'use strict';

function RectRenderable(stroke, fill, x, y, width, height) {
    this.stroke = stroke;
    this.fill = fill;
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
}